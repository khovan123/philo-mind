{
  "installCommand": "npm install",
  "buildCommand": "npm run shared:build && npm run build -w webapp",
  "outputDirectory": "webapp/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
